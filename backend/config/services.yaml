# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            $entityManager: "@doctrine.orm.default_entity_manager"

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    MarcusSports\:
        resource: '../src/'
        exclude:
            - '../src/**/Domain/'

    MarcusSports\Shared\Infrastructure\Command\DbInitCommand:
        tags: [ 'console.command' ]

    MarcusSports\Shared\Infrastructure\Command\DbTestCleanCommand:
        tags: [ 'console.command' ]
        arguments:
            $environment: '%kernel.environment%'

    MarcusSports\Users\Infrastructure\Controller\:
        resource: '../src/Users/Infrastructure/Controller'
        tags:
            - 'controller.service_arguments'

#    TeamEQ\Laravel\User\Infrastructure\:
#        resource: '../src/Laravel/User/Infrastructure/'
#        tags:
#            - 'controller.service_arguments'
#            - { name: 'monolog.logger', channel: 'domain_events' }

    MarcusSports\Users\Domain\Repository\UserRepository: '@MarcusSports\Users\Infrastructure\Persistence\UserRepositoryDoctrineMysql'
